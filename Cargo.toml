[package]
name = "vid-launcher"
version = "2.3.20"
edition = "2024"

[dependencies]
rand = "0.10.0"
iced = { version = "0.12", features = ["tokio"] }

[dev-dependencies]
tempfile = "3"

[profile.release]
opt-level = "z"
strip = true
lto = true
codegen-units = 1
panic = "abort"
