[package]
name = "vid-launcher"
version = "2.3.20"
edition = "2024"

[dependencies]
async-process = "2.5.0"
iced = "0.14.0"
rand = "0.10.0"

[dev-dependencies]
tempfile = "3"

[profile.release]
opt-level = "z"
strip = true
lto = true
codegen-units = 1
panic = "abort"
